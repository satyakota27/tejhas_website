/**
 * Site-wide config. Change logo path here if you use a different file (e.g. logo-new.png).
 * Replace public/logo.png with your actual logo asset.
 */
export const siteConfig = {
  logo: {
    src: "/logo.png",
    alt: "Tejhas",
    width: 220,
    height: 62,
  },
} as const;
